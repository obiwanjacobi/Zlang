#include "pch.h"

//#include "../../Zlang/zsharp/grammar/ZsharpParser.h"

TEST(TestCaseName, TestName) {
    EXPECT_EQ(1, 1);
    EXPECT_TRUE(true);
}

TEST(TestCaseName, AnotherName) {
    EXPECT_EQ(1, 0);
    EXPECT_TRUE(true);
}
